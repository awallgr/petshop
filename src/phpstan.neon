includes:
    - ./vendor/nunomaduro/larastan/extension.neon

parameters:

    paths:
        - app/
        - config/
        - resources/
        - routes/

    # Level 9 is the highest level
    level: 8
